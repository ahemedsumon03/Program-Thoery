<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sumonahemed.projectdemo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/binary"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SpalchScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity1"
            android:label="Login"
            android:theme="@style/FirstTheme"/>

        <activity
            android:name=".MainActivity"
            android:theme="@style/FirstTheme" />
        <activity
            android:name=".CInformationPage"
            android:label="Topic"
            android:theme="@style/FirstTheme" />
        <activity
            android:name=".JavaInformationPage"
            android:label="Topic"
            android:theme="@style/FirstTheme" />
        <activity
            android:name=".DetailsPage"
            android:label="Information"
            android:theme="@style/FirstTheme" />
        <activity
            android:name=".JavaDetailsPage"
            android:label="Information"
            android:theme="@style/FirstTheme" />
        <activity
            android:name=".FeedBackActivity"
            android:label="Feedback"
            android:theme="@style/FirstTheme" />
        <activity
            android:name=".AboutMeActivity"
            android:label="About"
            android:theme="@style/FirstTheme" />
        <activity
            android:name=".AbrevationActivity"
            android:label="Abrevation"
            android:theme="@style/FirstTheme" />
        <activity
            android:name=".QuizActivity"
            android:label="Quiz"
            android:theme="@style/FirstTheme" />
        <activity
            android:name=".CsharpInformationPage"
            android:label="Topic"
            android:theme="@style/FirstTheme" />
        <activity
            android:name=".CsharpDetailspage"
            android:label="Information"
            android:theme="@style/FirstTheme" />

    </application>

</manifest>